# Changelog

本插件不构建：`lib/` 是直接提交的产物。改代码后**刷新浏览器页面**即可生效，
只有改 `package.json` / `cordis.patch.yml` 才需要重启 `dsh web`。

## 0.1.0 — 2026-09-11

### 新增
- 侧边栏渲染为 **工作区 → [话题 → 子话题 → 会话] + 未归话题 + 其他**
- 话题归属于工作区（跨工作区拖拽被**显式拒绝**）
- 拖拽三义：拖到会话上/下半 = 排序；拖到话题标题 = 归入；拖到「未归话题」= 移出
- 话题操作：工作区行 `+` 新建、话题行悬停 `+` / `✎` / `×`（子话题 / 重命名 / 删除）
- 会话右键菜单：重命名（内联）/ 分叉 / 归档 / 移出话题；工作区右键：新建话题 / 重命名 / 删除
- 会话行相对时间（「刚刚 / N 分钟前 / N 小时前 / N 天前 …」，每分钟刷新）
- 展开态、话题树、归属关系、排序全部持久化（浏览器本地 store）
- 主存储 + `.bak` 备份，主存储为空时侧边栏顶部出现**恢复条**
- 离线冒烟测试 `test/smoke.mjs`（迷你 React 桩，无需浏览器/依赖）

### 取舍（有意的）
- **纯客户端**：host 半边 `lib/index.js` 是空壳、不 inject 任何服务 → 结构性零启动风险
- 分组数据存浏览器本地 → 不跨设备、清缓存即丢（用 `.bak` + 恢复条兜底）
- 以 `priority: -1` 顶替官方 `sidebar.workspaces` 浏览器 → 必须继承官方会话行的全部能力

### 文档
- `docs/HANDOFF.md`：给后续接手 agent 的交接文档（代码地图 / 契约 / 已知问题 / 路线）
